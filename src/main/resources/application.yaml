spring:
  application:
    name: send-box
  profiles:
    default: local

  datasource:
    url: jdbc:h2:mem:~/send-box
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none

symmetric:
  crypto:
    password: "1234567890123456"
    salt: "1234567890123456"
